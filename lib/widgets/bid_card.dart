import 'package:flutter/material.dart';
import 'package:tfg_auction/models/puja.dart';

class BidCard extends StatelessWidget {
  Puja puja;

  BidCard({Key? key, required this.puja}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
