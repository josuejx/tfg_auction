import 'package:flutter/material.dart';

class BidContent extends StatelessWidget {
  const BidContent({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('BidScreen'),
    );
  }
}
